This folder contains helper programs/scripts for launching jobs on the cluster.

They're not all "scripts" per-se since some of them are not interpreted; they
are compiled to a binary.  But they're all small programs that are intended to 
be easily modified/replaced. 
